---
title: "h2"
output: html_document
date: "2024-09-30"
---

```{r setup, include=FALSE}
library(readxl)
library(tidyr)
```

```{r}
MrT <- read_excel("TrashNew.xlsx", sheet = 1, skip = 1, na = c(NA,"",","), range = cell_cols("A:N")) |>
  janitor::clean_names() |>
  drop_na(dumpster)
MrT
ProfT <- read_excel("TrashNew.xlsx", sheet = 2, skip = 1, na = c(NA,"",","), range = cell_cols("A:N")) |>
  janitor::clean_names() |>
  drop_na(dumpster)
ProfT
```

